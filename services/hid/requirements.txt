# Runtime dependencies for hardware I/O and LCD image preparation. Pillow is
# imported lazily, so non-screen commands still work when it is unavailable.
# NOT installed by this service's build; install into a project-local venv
# (py -m venv .venv) before live verification.
hidapi==0.15.0
Pillow==11.3.0
